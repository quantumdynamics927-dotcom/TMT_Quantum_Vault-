name: Deploy to Hugging Face Spaces (Docker)

on:
  push:
    branches: [main]
    paths:
      - 'tmt_quantum_vault/**'
      - 'hf-deploy/**'
      - 'pyproject.toml'
      - 'vault_config.json'
      - 'metatron_geometry.json'
      - '.github/workflows/deploy-hf-docker.yml'
      - 'scripts/deploy_hf.py'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install "huggingface-hub>=0.20"

      - name: Deploy to Hugging Face Spaces
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          python scripts/deploy_hf.py \
            --space-name Quantum927/quantumvault \
            --private

      - name: Deployment summary
        run: |
          echo "## ✅ Docker Deployment Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Space URL**: https://huggingface.co/spaces/Quantum927/quantumvault" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**SDK**: Docker" >> $GITHUB_STEP_SUMMARY
          echo "**Port**: 7860" >> $GITHUB_STEP_SUMMARY
          echo "**Visibility**: Private" >> $GITHUB_STEP_SUMMARY
